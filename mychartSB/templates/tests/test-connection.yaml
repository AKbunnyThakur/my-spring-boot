apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mychartSB.fullname" . }}-test-connection"
  labels:
    {{- include "mychartSB.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mychartSB.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
