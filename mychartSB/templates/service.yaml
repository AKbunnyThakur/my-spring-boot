apiVersion: v1
kind: Service
metadata:
  name: {{ include "mychartSB.fullname" . }}
  labels:
    {{- include "mychartSB.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mychartSB.selectorLabels" . | nindent 4 }}
